---
import { Footerss } from '../content/data/db'
---

<footer
>
  <div
    class="rounded-lg w-full max-w-screen-xl mx-auto md:flex md:items-center md:justify-between py-6"
  >
    <span class="text-sm sm:text-center text-zinc-800/90 dark:text-zinc-200/90"
      >
    </span>
    <ul
      class="flex flex-wrap items-center mt-3 text-sm font-medium dark:text-white/90"
    >
    <p class="me-8">Contáctame:</p>
        {
          Footerss.map((foo) => (
          <foo.icon class="size-6 md-3 me-1" />
          <a href={foo.url} class="hover:underline me-4 md:me-8">{foo.name}</a>
        ))
      }
    </ul>
  </div>
</footer>